version: "3.8"
services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: postgres-nic
      POSTGRES_PASSWORD: o23unrc2opi2
      POSTGRES_DB: askymybook
    ports:
      - "5432:5432"
  web:
    build: .
    command: ./bin/prod
    volumes:
      - .:/askmybook
    ports:
      - "3333:3000"
    environment:
      OPENAI_API_KEY: SECRETHERE
    depends_on:
      - db
